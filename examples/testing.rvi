    nop
    nop
    addi $2, $0, 8

    nop
    nop
    sw $1, 128($0)
    sw $2, 132($0)
    sw $2, 136($0)
    sw $2, 140($0)
    lw $10, 52($0)
    lw $10, 68($0)
    lw $10, 8($0)
    lw $10, 12($0)
    lw $10, 16($0)
    lw $10, 8($0)
    lw $10, 12($0)
    lw $10, 16($0)
    lw $10, 20($0)
    nop
    lw $10, 20($0)
    sw $2, 16($0)
    lw $10, 4($0)
    nop
    lw $11, 4($0)
    add $1, $2, $4
    sltu $2, $1, $2
    add $5, $3, $5
    add $6, $2, $5
    nop
    nop
    nop
    nop
    nop
    addi $1, $1, 1
    addi $1, $0, 0              # loop counter
    addi $2, $0, 1
    addi $3, $0, 9              # loop limit
    addi $4, $0, 1              # fibonacci no. 1
    addi $5, $0, 1              # fibonacci no. 2
LOOP:
    addi $1, $1, 1              # counter++
    nop
    nop
    beq $3, $1, DONE            # if counter == loop limit then done!
    add $4, $4, $5
    add $5, $5, $4
    beq $0, $0, LOOP            # jump
DONE:
    sw $4, 8($0)
    sw $5, 12($0)
    lw $1, 128($0)
    addi $1, $1, 1
