    addi $3, $0, 100
    nop
    nop
LOOP:
    beq $3, $1, DONE            # if counter == loop limit then done!
    addi $1, $1, 1              # counter++
    sw $1, 0($1)
    beq $0, $0, LOOP            # jump
DONE:
    nop
    nop
    nop
    nop
    nop
